/**
 * 作用：Next.js 构建与运行配置。
 * 使用位置：next dev / next build / next start 启动时自动读取。
 * 输入：文件内直接书写的配置项（可读取 process.env）。
 * 输出：导出 NextConfig 对象供 Next.js 使用。
 */
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
